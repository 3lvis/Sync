version: 2
jobs:
  build-and-test:
    macos:
      xcode: "9.3.0"
    steps:
      - run: xcrun instruments -w "iPhone X (11.3) [" || true
      - run: xcodebuild -project 'Project.xcodeproj' -scheme 'iOSTests' clean build test -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone X,OS=latest' | xcpretty --color

workflows:
  version: 2
  build-and-test:
    jobs:
      - build-and-test      
