version: 2
jobs:
  build-and-test:
    macos:
      xcode: "9.3.0"      
    steps:
      - run:
          name: Build and run tests
          command: fastlane scan
          environment:
            SCAN_DEVICE: iPhone X
            SCAN_SCHEME: iOSTests
workflows:
  version: 2
  build-and-test:
    jobs:
      - build-and-test      
